import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.precisionarchitect.app',
  appName: 'Precision Architect',
  webDir: 'dist'
};

export default config;
